$('#new_comment').remove();
$('.reply').show();
$('.add_comment<%= params[:comment_id] if params[:comment_id].to_i > 0 %>').parent().hide().parent().after('<%= j render('form') %>');
$('.add_comment<%= params[:comment_id] if params[:comment_id].to_i == 0 %>').parent().after('<%= j render('form') %>');